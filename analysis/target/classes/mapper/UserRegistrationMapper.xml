<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mapper.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.jlysh.analysis.pojo.mapper.UserRegistrationMapper">
    <insert id="saveToDataBase" parameterType="com.jlysh.analysis.pojo.beans.UserInfo">
        insert into sys_user(num,name,pwd,email,phone)
        values(#{num},#{name},#{pwd},#{email},#{phone})
    </insert>

</mapper>